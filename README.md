# Python-Assignment-4
Practice and Learning

# TASK 1 
  * created a file name sample.txt . added some text into it.
  * opened the file using with in read mode (rt) and using for loop for printing its content line by line.
  * Uses Try and Except functions for for Error handling .
  * Here , FILENOTFOUND error is handled  in Except if mentioned file is not present.


# TASK 2
 * First created the file using (xt or wt). here (wt) is used to write data into it.
 * Add some text into it .then, asks to append or data additional data usin (at).
 * then open the file with read mode (wt) and prints its all content using for loop. 

